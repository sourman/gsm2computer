# Optional: workspace-scoped API key instead of OAuth (see config/mcporter.json).
# Create at https://linear.app/safwatly/settings/api
# LINEAR_API_KEY=lin_api_...

# Default: OAuth once per repo (tokens in .mcporter/oauth/linear, gitignored):
#   mcporter auth linear
# Pick safwatly in the browser (not compliancy-group).
